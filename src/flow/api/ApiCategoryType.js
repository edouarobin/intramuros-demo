// @flow
/* eslint-disable no-undef */

declare type ApiCategoryType = {
  id: number,
  name: string,
  description: string,
  image: string,
  default: boolean,
  picto: string,
  theme_id: number,
  theme_name: string,
  theme_order: number,
};
