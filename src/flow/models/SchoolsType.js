// @flow
/* eslint-disable no-undef */

declare type SchoolsType = {
  name: string,
  city: number,
  id: number,
  image: string,
  addressLabel: string,
  latitude: number,
  longitude: number,
  cityName?: string,
  distanceToSelectedCity?: number,
  distanceDisplay?: string,
};
